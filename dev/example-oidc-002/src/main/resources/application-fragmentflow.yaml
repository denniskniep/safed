server:
  port: 8086

spring:
  application:
    name: ExampleOidcApp
    title: Example Oidc App - Fragment Flow

  security:
    oauth2:
      client:
        registration:
          example-oidc-002-fragmentflow:
            # todo: pkce!
            client-id: example-oidc-002-fragmentflow
            client-secret: 6N5fnpxRJ18fmu9qOVGZwYN5hA6P2gDx
            redirect-uri: http://localhost:8086/fragment.html
            scope:
              - openid
              - profile
              - email
        provider:
          example-oidc-002-fragmentflow:
            issuer-uri: http://keycloak:8080/realms/demo
